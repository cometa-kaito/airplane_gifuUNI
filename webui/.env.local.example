# この WebUI は WebSerial (USB 直結) 専用です。
# 動作に必要な環境変数はありません（このファイルは不要）。
#
# 旧 WebSocket 経路（Python ground_station.py 経由, NEXT_PUBLIC_WS_URL /
# NEXT_PUBLIC_WS_TOKEN）は archive/websocket/ へ退避しました。
# 復元する場合は archive/websocket/README.md を参照してください。
